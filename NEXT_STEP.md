# Next Step

在 `/Users/cory/codes/Kory_whisper/.worktree/audio-cues` 启动应用并完成一次组合冒烟验证：确认默认是开始录音 `Tink`、输出完成 `Glass`，在设置页切换声音后保存能立即生效，同时日志中的 `Models directory` 指向 `~/.kory-whisper/models` 且不会因切换 worktree 再次提示下载已有 Whisper 模型。
