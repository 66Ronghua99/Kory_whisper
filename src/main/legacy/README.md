# Legacy runtime modules

These modules were superseded by platform adapters and are kept for historical reference only:

- `audio-recorder-legacy.js`
- `input-simulator-legacy.js`

Current runtime path for input simulation and audio capture is `src/main/platform/`.
